---
const { compact = false } = Astro.props;
---

<header
  class:list={[
    "w-full flex justify-center",
    compact ? "max-w-6xl mx-auto mt-5 mb-4 px-4" : "max-w-7xl mx-auto my-10",
  ]}
>
  <img
    src="/logo.png"
    width={compact ? 168 : 256}
    height={compact ? 84 : 128}
    alt="LuminArte"
    class="drop-shadow-md"
  />
</header>
